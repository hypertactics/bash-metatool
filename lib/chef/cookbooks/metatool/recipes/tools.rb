# download via git a set of tools to have metatools wrap around for
# workflow management

# IDEA: download via git, remote_file or other to get a set of tools 

node['metatool']['tools'].each do
  
end
